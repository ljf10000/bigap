function printo (name, obj) {
	for (var p in obj) {
	        print(name + "." + p + " = " + obj[p]);
	}
}

function __debugger(level) {
	print("debugger level=", level, arguments.slice(1).toString())
	
//	__my__.debugger(level, arguments.slice(1).toString());
}
