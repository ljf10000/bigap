function printo (name, obj) {
	for (var p in obj) {
	        print(name + "." + p + " = " + obj[p]);
	}
}

function __debugger(level) {
	var left = Array.prototype.slice.call(arguments).slice(1)
	
	print("debugger level=", level, left.toString())
	
//	__my__.debugger(level, left.toString());
}
