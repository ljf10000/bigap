#!/bin/bash

main() {
	local flag="error"
	local path="/tmp/$(whoami)/ak"
#	local flag="*"

	echo "debug ${flag}" > ${path}/jlogd.key
	echo "debug ${flag}" > ${path}/tmd.key
	echo "debug ${flag}" > ${path}/smd.key
	echo "debug ${flag}" > ${path}/umd.key

	../../app/ak/ak reload
}

main "$@"
